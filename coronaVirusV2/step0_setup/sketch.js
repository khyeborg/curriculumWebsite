// // program variables
// let computerScienceContentArray = document.getElementsByClassName("computer_science_content_page");

// function windowResized() {
// 	console.log(computerScienceContentArray);
//    // resizeCanvas(computerScienceContentArray[1].offsetWidth - 200, windowHeight - 200);
// }

function setup() {
   // let canvasStep0 = createCanvas(computerScienceContentArray[0].offsetWidth - 200, windowHeight - 200);
   // canvasStep0.parent('sketch0');
   createCanvas(1000, 500);
   noStroke();
   noiseDetail(24);
}
 
function draw() {
   background(0);
}